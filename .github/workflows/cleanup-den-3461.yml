name: cleanup-den-3461
on:
  push:
    branches: [agent/den-3461-user-dashboard]
permissions:
  contents: write
concurrency:
  group: format-den-3461
  cancel-in-progress: false
jobs:
  format-and-clean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all
      - name: Commit formatting and remove temporary workflows
        run: |
          git config user.name "embedded-alerts-ci"
          git config user.email "actions@users.noreply.github.com"
          git add src
          git rm --ignore-unmatch .github/workflows/format-den-3461.yml
          git rm --ignore-unmatch .github/workflows/cleanup-den-3461.yml
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style(DEN-3461): apply rustfmt and remove temporary formatter"
          git push origin "HEAD:${GITHUB_REF_NAME}"
