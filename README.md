# eal-leptos-web

Leptos Embedded Alerts web server using shared interfaces and alert-domain contracts.

Initialized through `DEN-1949` as a testable `leptos` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
